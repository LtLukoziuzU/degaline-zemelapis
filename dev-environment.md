---
name: Developer environment — installed tools
description: Tools confirmed installed on the developer's system (CachyOS/Arch Linux), so Claude does not need to ask again
type: reference
---

# Developer Environment

**OS:** CachyOS (Arch Linux base), Fish shell

## Confirmed installed

| Tool | Install command used |
|---|---|
| `python-openpyxl` | `sudo pacman -S python-openpyxl` |
| `curl` | (system default) |
| `xdg-open` | (system default) |
| `wl-clipboard` (`wl-copy`/`wl-paste`) | `sudo pacman -S wl-clipboard` |
| `gh` (GitHub CLI) | `sudo pacman -S github-cli` |
